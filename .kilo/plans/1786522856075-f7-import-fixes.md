# F7 Completion Plan - Import Path Fixes

## Issue
Build fails with 5 unresolved import errors in admin booking pages due to incorrect relative paths.

## Root Cause
Admin pages are at `src/pages/` level, but imports use `../../` (expecting `src/pages/admin/` structure).

## Files to Fix

### 1. `src/pages/AdminBookings.jsx`
- Line 3: `../../services/bookingService` → `../services/bookingService`
- Line 5: `../../components/bookings/BookingStatus` → `../components/bookings/BookingStatus`

### 2. `src/pages/AdminBookingDetails.jsx`
- Line 3: `../../services/bookingService` → `../services/bookingService`
- Line 4: `../../components/ui/Button` → `../components/ui/Button`
- Line 5: `../../components/bookings/BookingStatus` → `../components/bookings/BookingStatus`

## Validation Steps
1. Run `npm run build` in frontend directory - should pass
2. Run `npm run dev` - verify no console errors
3. Test client flows: `/bookings`, `/bookings/:id`, cancellation
4. Test admin flows: `/admin/bookings`, `/admin/bookings/:id`, status updates
5. Verify responsive design on mobile/desktop
6. Verify dark/light theme works

## Out of Scope
- No new features - only fixing import paths
- Backend is already complete (Phase 9)